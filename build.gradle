apply plugin: 'java'
apply plugin: 'application'

mainClassName = "Exercise3"

repositories {
  mavenCentral()
}

dependencies {
  testCompile 'junit:junit:4.12'
}

run {
  try {
    systemProperties System.getProperties()
    args System.getProperty("args").split()
  }
  catch(Exception e){}
}

task sequencehelp <<
{
  println "1. Print \"Integer Sequence Project\""
  println "2. Print \"Given an integer n as an argument, displays Tri(n) and LC(n)\""
}
